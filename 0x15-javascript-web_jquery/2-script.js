// When the document is ready
$(document).ready(function () {
  // On click of the #red_header element
  $('#red_header').click(function () {
    // Update the text color of the header to red
    $('header').css('color', '#FF0000');
  });
});

